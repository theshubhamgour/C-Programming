// We will try to taks input from the user so that it will be interactive

// For addtion:

#include <stdio.h>

int main(){
    int a,b,c;
    printf("Enter two numbers:\n");
    scanf("%d%d",&a,&b);
    c= a+b;
    printf("The addition of two number is: %d",c);
    return(0);

}

/*

For Multipication:

#include <stdio.h>
int main(){
    float a,b,c;
    printf("Enter two values: \n");
    scanf("%f%f",&a,&b);
    c=a*b;
    printf("The product of two numbers are: %f",c);
    return(0);
}

*/



/*

For Division:

#include <stdio.h>
int main(){
    float a,b,c;
    printf("Enter two values: \n");
    scanf("%f%f",&a,&b);
    c=a/b;
    printf("The Division of two numbers are: %f",c);
    return(0);
}

*/